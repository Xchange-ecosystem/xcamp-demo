{
  "buildCommand": "cd submodules/xcamp-sdk && pnpm install && pnpm --filter @xchange/client build && cd ../.. && bun run build",
  "installCommand": "bun install",
  "outputDirectory": "dist",
  "git": {
    "submodulesCloneOnPreview": true
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
