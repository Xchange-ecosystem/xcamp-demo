{
  "buildCommand": "cd submodules/xcamp-sdk/packages/client && npm install && npm run build && cd ../../.. && bun run build",
  "installCommand": "bun install",
  "outputDirectory": "dist",
  "git": {
    "submodulesCloneOnPreview": true
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
