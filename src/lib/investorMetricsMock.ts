// Mock values for the investor persona's Project Home metrics row (Phase 5).
// Only "Progress" is real (ProjectDetailMetrics, from the live DB) — Quality,
// Proof and Members tracking don't exist yet, so these are illustrative
// placeholders until that data is wired up.
export const MOCK_QUALITY_PCT = 87; // MOCK
export const MOCK_PROOF_TOTAL = 128; // MOCK
export const MOCK_PROOF_AVG_PER_TASK = 3.4; // MOCK
export const MOCK_MEMBERS_COLLABORATORS = 8; // MOCK
export const MOCK_MEMBERS_VIEWERS = 4; // MOCK
